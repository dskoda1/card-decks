{
  "name": "card-decks",
  "version": "0.2.0",
  "description": "A module to simulate one to n decks of cards.",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha test/*.js",
    "coverall": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "keywords": [
    "cards",
    "deck of cards",
    "decks"
  ],
  "author": "dskoda1",
  "repository": "https://github.com/dskoda1/card-decks",
  "license": "MIT",
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.13",
    "eslint": "^3.5.0",
    "gulp-eslint": "^3.0.1",
    "gulp-mocha": "^3.0.1",
    "mocha": "^3.0.2",
    "mocha-lcov-reporter": "^1.2.0"
  },
  "dependencies": {
    "lodash": "^4.15.0"
  }
}
